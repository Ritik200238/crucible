node_modules/
dist/
.env
*.log
.guardrail/

# local agent tooling, not part of the project
.claude/
.gstack/

# competitor code — never enters this repo (licence contamination)
competitors/

# reference material — read-only, never enters this repo (LGPL)
reference/

# ledger and its signing key — never committed
.crucible/
# sampler process log; the samples themselves are the evidence and are kept
data/sampler.log
crucible.config.json

# A signing key must never be built into an image.
deploy/ledger/ledger-key.json
